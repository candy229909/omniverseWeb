name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      DATABASE_URL: file:./ci.db
      SESSION_SECRET: ci-secret-must-be-at-least-32-characters-long

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        # `--no-optional` skips @nvidia/omniverse-webrtc-streaming-library,
        # which lives on NVIDIA's private artifactory and isn't reachable
        # from GitHub-hosted runners without an auth token.
        run: npm ci --no-optional

      - name: Type check
        run: npx tsc --noEmit

      - name: Prisma generate
        run: npx prisma generate

      - name: Build
        run: npx next build
